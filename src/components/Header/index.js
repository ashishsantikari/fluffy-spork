import React from 'react';
import Box from '../Box';
import Text from '../Text';

const Header = () => (
  <Box as="section">
    <Text as="h1" textTransform="uppercase">
      Repo Viewer
    </Text>
  </Box>
);

export default Header;
